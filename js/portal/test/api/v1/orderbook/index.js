/**
 * @file Behavioral spec for the orderbook
 */

module.exports = function (client, server) {
  describe('Order Book', function () {
    describe('PUT', function () {
      it('must create a new order in the order book')
    })

    describe('GET', function () {
      it('must retrieve an order in the order book')
    })

    describe('DELETE', function () {
      it('must cancel an order in the order book')
    })

    describe('UPGRADE', function () {
      it('must watch the order book for changes')
    })
  })
}
